{% extends '::base.html.twig' %}

{% block body -%}
    <h1>carrito creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('carrito') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
