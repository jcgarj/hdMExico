{% extends '::base.html.twig' %}

{% block body -%}
    <h1>pais creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pais') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
